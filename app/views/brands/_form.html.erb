<%= form_with(model: brand) do |form| %>
  <% if brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

      <ul>
        <% brand.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p class="p_form">Название бренда</p>
    <%= form.text_field :name , placeholder: "Введите название"%>
  </div>

  <div class="field">
    <p class="p_form">Ссылка на бренд</p>
    <%= form.text_field :link, placeholder: "Введите ссылку" %>
  </div>

  <div class="actions button_form">
    <%= form.submit "Сохранить" %>
  </div>





<% end %>
