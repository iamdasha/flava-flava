<% set_meta_tags title: 'Артисты', description: 'Артисты на сервисе Flava'%>
<% set_meta_tags og: {
    title: ['Артисты']
}  %>



<div class="index_handb">
</div>

<% if user_signed_in? && current_user.isadmin == true %>
    <%= react_component('O_HeaderAdmin', current_user: current_user)  %>
    <div class="titleadminblock">
      <h1 class="titleadmin">Артисты</h1>
      <%= link_to 'Создать', new_artist_path, :class =>"link_newpost", :class =>"link_new" %>
    </div>

    <%= react_component 'O_AllArtistsAdmin', { artists: @artistcoms, filtertags: @filtertags}  %>
<% else %>
  <%= react_component('O_Header', current_user: current_user)  %>
<% end %>

<%= react_component 'O_Footer'  %>
