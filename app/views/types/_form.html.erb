<%= form_with(model: type) do |form| %>
  <% if type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
        <% type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p class="p_form">Название категории</p>
    <%= form.text_field :name , placeholder: "Введите название"%>
  </div>

  <div class="actions button_form">
    <%= form.submit "Сохранить" %>
  </div>


<% end %>
